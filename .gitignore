*.o
*.ko
*.mod
*.mod.c
*.mod.o
*.symvers
*.order
.tmp_versions/
.*.cmd
Module.symvers
modules.order
userspace/avctl/avctl
userspace/avd/avd
tests/netlink_test_helper
av/compile_commands.json
tests/qemu-boot/init
tests/qemu-boot/cold_launcher
tests/qemu-boot/root/
tests/qemu-boot/initramfs.cpio.gz
tests/qemu-boot/serial.log
# Cached cross-compiled arm64 kernel tree, built by
# tests/test_detection_qemu.sh so a from-scratch defconfig build
# doesn't have to happen on every push that touches av/.
tests/qemu-boot/.kernel-cache/
packaging/arch/pkg/
packaging/arch/src/
packaging/arch/*.pkg.tar.*
packaging/arch/hyprav/
packaging/flatpak/.flatpak-builder/
packaging/flatpak/build-dir/
packaging/appimage/AppDir/
packaging/appimage/*.AppImage
# packaging/flatpak/README.md's own build instructions (and
# .github/workflows/release.yml's build-flatpak job) run
# flatpak-builder from the repo root, not from packaging/flatpak/ -
# same build-dir/.flatpak-builder cache dirs as above, plus the
# exported repo and bundled .flatpak, just rooted here instead.
/.flatpak-builder/
/build-dir/
/repo/
/*.flatpak
